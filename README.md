# UrlShortener

## Run the solution

Navigate to the project folder e.g.: `\UrlShortener.API-master\UrlShortener.API` and run "dotnet run" from the command line

The application should start in localhost:PORT URL.

### SwaggerUI
`http://localhost:xxxx/swagger`

### URL redirect
`http://localhost:xxxx/uniqueID`

